# ReguLens — Progress Tracker

**Single source of truth for what is done.** Check here before starting any work.

Last updated: **31 Aug 2026** · Updated by: **the stage cleanup and truth pass**

> **Read this first, every session.** If a phase below is `COMPLETE`, do not rebuild
> it — read its phase file to see what exists. If it is `IN PROGRESS`, the unticked
> boxes in that phase file are the remaining work.

## Legend

| Marker | Meaning |
|---|---|
| `[ ]` | Not done |
| `[x]` | Done, exists in the repo, and deployed |
| `[~]` | Deliberately skipped — the line says why. **Do not re-litigate.** |
| `NOT STARTED` / `IN PROGRESS` / `COMPLETE` / `SKIPPED` | Phase-level status |

A box is ticked only when the thing is real. Not "written in a doc", not "mostly
working locally". If it is not deployed and not verified, it stays unticked.

## Phases

| # | Phase | Planned | Status | Started | Completed |
|---|---|---|---|---|---|
| 0 | [Foundation](phases/phase-0-foundation.md) | Aug 19–20 | `COMPLETE` (web hosting moved to Cloud Run; the Vercel and push-trigger boxes are skips with reasons, not gaps) | 19 Aug | 31 Aug |
| 1 | [Compliance Twin](phases/phase-1-compliance-twin.md) | Aug 21 | `COMPLETE` (frontend deployed on the Cloud Run web service; no box in the phase file is left unticked) | 19 Aug | 29 Aug |
| 2 | [Ingestion & Extraction](phases/phase-2-ingestion-extraction.md) | Aug 22–23 | `COMPLETE` | 22 Aug | 23 Aug |
| 3 | [Guardrail & Reconciliation](phases/phase-3-guardrail-reconciliation.md) | Aug 24–25 | `COMPLETE` | 23 Aug | 23 Aug |
| 4 | [Impact Engine](phases/phase-4-impact-engine.md) | Aug 26–27 | `COMPLETE` (latency re-measured 29 Aug: 25.5s for one pasted rule, 174.3s for a 55-clause annex, vs a 90s target) | 23 Aug | 23 Aug |
| 5 | [Timeline & Query](phases/phase-5-timeline-query.md) | Aug 28–29 | `COMPLETE` (10/10 grounding check live) | 23 Aug | 23 Aug |
| 6 | [E2E Testing](phases/phase-6-e2e-testing.md) | Aug 30 | `IN PROGRESS` (UC-A..F + redelivery/concurrency/DLQ/walker/grounding all live-green; formal Playwright shell left) | 23 Aug | — |
| 7 | [Hardening & Submission](phases/phase-7-demo-hardening.md) | Aug 31 | `IN PROGRESS` (deployed and exercised live 29 Aug; submission package outstanding) | 26 Aug | — |
| 8 | [Watched Sources](phases/phase-8-live-monitoring.md) | Aug 29 | `COMPLETE` (deployed; the scheduler fired, and both change-watching and discovery are proven live) | 29 Aug | 29 Aug |
| 9 | [Country Discovery](phases/phase-9-country-discovery.md) | Aug 31 | `COMPLETE` (deployed; Singapore and Japan each committed a real watched source from Cloud Run, 52 tests) | 31 Aug | 31 Aug |

## Cross-cutting work

Tracked here because it spans phases and is easy to lose.

- [x] `trace_id` propagated end to end (phase 0) — API mints/adopts, publisher stamps, worker re-adopts; verified by a Cloud Logging query
- [x] Structured JSON logging everywhere, no `print` (phase 0, maintained throughout)
- [x] Cloud Build pipeline green, rollback practised (phase 0) — manual trigger by design for a single-maintainer hackathon build; a push trigger needs GitHub-app OAuth from the repo owner and buys nothing before the deadline. Wiring it is post-hackathon work
- [x] Secret Manager wired, no secrets in image or repo (phase 0) — `gemini-api-key` mounted via `--set-secrets` in cloudbuild.yaml for api + worker; `.gitignore` audited 29 Aug; no key literals in source; a placeholder value is treated as no key at all
- [x] Budget cap + billing alert live (Rp 540,000 ≈ $30/month, 50/90/100%) — *email channel still needs the user to click Google's verification mail*
- [x] `docker compose up` brings up the full local stack (phase 0) — verified 25 Aug;
      `scripts/verify_local.sh` runs the whole drill offline, free, in ~2 minutes
- [~] Five alerts configured **and each one deliberately triggered once** (phase 0 → 7) — SKIPPED: budget alert + uptime check live and enough for this coverage; five policies with deliberate triggers do not fit before the deadline and add no evidence `scripts/verify_e2e.sh` has not already shown
- [x] Debug view `/debug/documents/{id}` (phase 2, extended in 3) — behind
      `DEBUG_VIEW`, live: stage_log, rejected candidates, reconciliation
      decisions, confidence components
- [x] Frontend hosting on Cloud Run (`regulens-web`) — Vercel unblocked; CORS
      pinned to the web origin in cloudbuild.yaml
- [x] `data-testid` attributes added as UI is built (phases 1–5) — started in phase 0's home page
- [x] `make test-all` — one command runs lint, unit tests, type check, a
      production web build and the full local emulator drill, and prints a
      pass/fail table. Green from a clean checkout, no GCP, no cost
- [~] `firestore.indexes.json` committed and submitted by `scripts/setup.sh` —
      SKIPPED, and this box was wrong until 31 Aug. The file is committed; nothing
      was ever submitted, because `scripts/setup.sh` ran
      `gcloud firestore indexes create --file=`, which is not a command — that
      group only has `composite` and `fields` — and both branches of the `||`
      printed a reassuring sentence, so a step that could never work reported
      success on every run. Nothing missed them: `gcloud firestore indexes
      composite list` returns nothing on the deployed project and every query
      works, because Firestore serves several equality filters from its automatic
      indexes and the two queries that would otherwise sort or range read a
      bounded page and sort in process on purpose. The script now says "none
      required" instead of pretending, and the file stays as the record of where
      one would become necessary
- [x] `min-instances=1` on the two services a person opens, so the first request
      after a quiet hour does not pay a container start
- [x] Cloud Scheduler job `regulens-source-check` live and observed firing (phase
      8) — daily 06:00 Asia/Jakarta, OIDC as `regulens-pubsub-invoker`. Run by
      hand twice against the deployed worker: the first run ingested Commission
      Regulation (EU) 2023/2108 and 68 clauses came out of it; the next check
      returned `not_modified`
- [x] Every repository mutation writes a `graph_event` (phase 1 onward) — same-batch write, no raw update exposed
- [x] Every Pub/Sub handler idempotent, redelivery tested — extract, reconcile
      and impact all verified against the deployed stack; the concurrent
      probe additionally caught and fixed a double-race hole
- [x] Extraction fixture set + accuracy test checked in — verbatim corpus
      excerpts, live-Vertex run gated behind `REGULENS_EVAL=1`, result 5/5
- [~] `FAKE_LLM=1` fixtures covering the E2E suite — SKIPPED as a separate fixture set: `llm.fake_candidates` is now keyed on the document's own text (Indonesian source → 400 mg/kg, EU source → 150 mg/kg), which is enough to drive the entire local E2E drill including the conflict and the flip. A second fixture layer would duplicate it

## Decisions taken

Recorded so they are not reopened. Change one only with a reason written here.

- [x] **A name the dictionary has since learned is not a judgement call, 31 Aug**
  — `substance_not_recognized` joins the reasons a recheck can settle, but
  conditionally, and the condition is the whole design: the same strict matcher
  runs again over the substance the document stated, and only a name it now
  recognises is released. A name it still refuses stays refused. Nothing
  relaxes the matcher and nothing invents a mapping — a wrong normalization
  silently compares two different substances. `low_confidence_or_flagged` is
  allowed to sit alongside it and only above the floor, because that name is
  two refusals in one: confidence under the floor, or extraction flagged the
  row, and an unrecognised substance is what raises the flag. A clause actually
  under the floor is refused exactly as before
- [x] **A row states its food in words where it prints no code, 31 Aug** — the
  GSFA code fixed the BPOM tables; the EU annexes carry no code and say it
  after `only` or `except` instead. `scope.py` reads that phrase and reports
  it; it does not decide that Wiltshire bacon is a cured meat. Refusing an
  unmatched pair is the cautious direction: nothing is superseded, both rows
  stay active, and a product is still measured against the stricter. The
  alternative is a supersede between two rows about different meats, which
  deletes a limit that still binds
- [x] **An amendment is two rows and they say which is current, 31 Aug** —
  Annex II writes the replaced text as `Period of application: until 9 October
  2025` and its replacement as `from 9 October 2025`. Only a `from` date is an
  effective date, so the replaced row arrives dated `None` and the pair looks
  undecidable. `applies_until` reads the end of the period from the text, which
  turned sixteen model calls into sixteen deterministic supersedes
- [x] **A ceiling that names no food is not a food limit, 31 Aug** — the purity
  tables of Regulation 231/2012 read "Not more than 20 mg/kg expressed as
  KNO2", naming no category and no scope. At 20 mg/kg it is the strictest
  nitrite number in the annex, so left active it binds every cured meat in the
  graph and fails all of them on a specification. An Annex II food limit always
  says which food it is about; one that does not goes to a person
- [x] **A rule that is active can be taken back out, 31 Aug** — `dismiss`
  guarded on `needs_review`, so a clause that reached the graph before the code
  learned to refuse it could only be removed by deleting its whole document.
  Found in production: a laboratory drying method reported as the 2 500 mg/kg
  limit binding a cured sausage. Withdrawal now carries the duty a document
  delete carries — the requirements it wrote and the conflicts it opened go
  with it, and every product it touched is re-evaluated. `superseded` stays
  out: that is a decision reconciliation made, not a rule anybody is acting on
- [x] **A requirement does not outlive the rule it came from, 31 Aug** —
  materialization walks the *active* clauses, so a requirement whose clause was
  superseded is never revisited and never corrected, and nothing removed it, so
  the rollup kept counting it. A cured sausage reformulated to 20 mg/kg stayed
  `non_compliant` against a nitrite limit superseded minutes earlier, recorded
  against the 120 mg/kg it no longer contained. That is a verdict drawn from a
  limit that is not the one in effect — the error `rollup_status` already
  refuses to make when it filters by effective date, arriving through the
  requirement instead of the clause. Requirements whose clause is no longer
  active are retired on every materialization; the clause and its events stay
- [x] **An alert names the rule the verdict rests on, 31 Aug** — impact was
  given the clause whose change *started* the run and stamped that as the
  cause, so an alert could read "it sets nitrates at 150 mg/kg" above a verdict
  about 30 mg/kg of nitrites: same regulation, wrong row. The cause is now the
  requirement the verdict rests on — worst first, in force only, which is
  `_status_from`'s own rule asked for the row instead of the word — and the
  product-edit path names it too, where it used to record none at all and the
  UI said "the rule behind this has since been removed" about a rule nobody had
  removed
- [x] **The cause of a verdict is a document, not only a clause, 31 Aug** —
  `graph.changed` carried the clause alone, so `cause.document_id` was null on
  every verdict reconciliation ever moved, and `unprompted` — the single claim
  separating this from a checker — defaulted to "somebody uploaded it". The
  message now carries the document, and alerts resolve it through the clause on
  read so events already written answer correctly without a backfill
- [x] **A queue may empty itself, but only where typed code can answer, 31 Aug**
  — thirty-six rows of one BPOM table sat waiting for a person to confirm that a
  regulation does not contradict itself, which is not a judgement call, it is a
  missing field. Two answers were available: a bulk-accept button, or the fact
  the rows already state. The button would have been a person clicking through
  thirty-six decisions they had no way to check; the fact is the food category
  the regulator prints at the head of every row, and it settles the question
  deterministically. So the guardrail learned to read it, and `/clauses/recheck`
  re-decides the backlog through the ordinary reconciliation path. The line that
  must not move: only `judge_ambiguous` is ever reopened. That reason means
  "typed code had nothing to go on", which is exactly what changed. Low
  confidence and low authority stay a person's job forever — no recheck makes an
  unreadable number readable, and an app that cleared those would be claiming a
  confidence it does not have
- [x] **The word "monitoring" now describes behaviour, 29 Aug** — until this
  point nothing entered the graph unless a person uploaded it, which made the
  honest word "checker". `/sources` re-reads a regulator's own address on a
  schedule and ingests a change through the ordinary upload path. Either the
  word had to go or the feature had to exist; the feature was the smaller change
- [x] **A change is a change of wording, not of bytes, 29 Aug** — EUR-Lex stamps
  a fresh session id into every response, so a byte hash reports a change on
  every fetch and bills a model run for it. The signal is a hash of the
  extracted text; conditional GET short-circuits ahead of it where a server
  sends validators
- [x] **The EU source is watched through CELLAR, not the EUR-Lex page, 29 Aug** —
  the EUR-Lex HTML URL works from a laptop and was verified locally three times.
  Deployed, it answered Cloud Run with `202` and a challenge page, and the first
  scheduled sweep recorded an error. The Publications Office's machine-readable
  endpoint serves the same 48,417 characters and sends an `ETag`. What works
  from a laptop is not evidence that it works from Cloud Run
- [x] **Both genai clients own their transport, 29 Aug** — `BaseApiClient` closes
  its httpx client on garbage collection unless the caller supplied one. Without
  that, the direct extraction fallback died on a transport a finished ADK runner
  had shut, and a document with a working path left was recorded `failed`. Seen
  in production on 23, 28 and 29 Aug; the fix is to hand both clients a
  transport this process keeps
- [x] **Watching a known address cannot find an unknown regulation, 29 Aug** —
  a `document` source reports only that a rule already held was edited. A rule
  published tomorrow arrives at an address nobody has seen. `listing` closes it:
  a regulator's index page plus a required pattern saying which links are
  regulations, with any unseen link treated as a new document. The pattern is
  required because an index also links to news, social media and a language
  switcher, and following all of them would ingest the website. A pattern that
  stops matching is an error, not "no new regulations" — a site redesign and a
  quiet month must not look the same
- [x] **A permanent refusal is remembered, a transient one is not, 29 Aug** —
  BPOM's 308-page annex will be 308 pages tomorrow, so refusing it is final and
  the item is marked seen; a timeout or an empty page is not, because EUR-Lex
  serves empty challenge pages and abandoning a regulation over one night is
  worse. Without the split, one oversized PDF is re-downloaded and re-refused
  nightly, holding a slot in the per-run cap a readable regulation needed
- [x] **Discovery is asked, not scraped, where the publisher offers it, 29 Aug**
  — a listing depends on a regex still matching a page a designer may rewrite; a
  catalogue query depends on the publisher's classification of its own acts,
  which is what that classification is for. The EU both offers one and requires
  it, since EUR-Lex's web pages refuse datacentre addresses. Scope measured
  before it was written down: 133 works a year carry the EuroVoc food-additive
  concept, of which the CELEX shape filter leaves roughly a dozen regulations
- [x] **A catalogue query looks back a fixed window, 29 Aug** — not "since the
  last check". A missed run, a clock skew or a restored backup would open a gap
  nobody notices; re-asking for the same 120 days is free, because everything
  already read is already remembered
- [x] **A rule that cannot apply to you is not put in front of you, 30 Aug** —
  watching whole regulations put 200 entries in the review queue, every one of
  them the guardrail behaving correctly and 164 of them about ingredients
  nothing in the workspace contains. Relevance is computed on read, never
  stored: nothing is deleted or downgraded, a held-back rule applies to a
  product added tomorrow with no migration, and the queue always states how many
  it is holding back and why. Generous by construction — an unstated substance
  or product type still binds, because hiding a rule that mattered is a wrong
  verdict while showing one that did not is a line of text
- [x] **An alert has to say why, 30 Aug** — the banner read "changed by a rule
  you added, without being asked", which was wrong in both directions: wrong for
  a document the user had just uploaded, and wrong for a regulation that arrived
  overnight from a watched address, which nobody added. The cause is now
  resolved from stored records into the facts behind it, and a cause that has
  been deleted says so rather than borrowing another one's story
- [x] **Autonomy is counted, not asserted, 30 Aug** — `GET /stats/autonomy`
  reports regulations found unprompted, rules read out of them and verdicts
  moved, each figure a query over the same collections the rest of the app
  reads. A quiet week reports zeros; it is the easiest number here to inflate
- [x] **Google ADK required** — all four agents are ADK agents; tool bodies stay plain functions
- [x] **Pub/Sub + Cloud Run Jobs approved** — three topics, one per pipeline stage
- [x] **Cloud Build** for CI/CD, not GitHub Actions
- [x] **Secret Manager** for secrets; plain env vars for non-secret config
- [x] **Docker Compose** for local, with push subscriptions matching production
- [x] **Vertex topology** — infra in `asia-southeast1`, Gemini `gemini-3.5-flash` via the `global` endpoint, embeddings `text-multilingual-embedding-002` in `asia-southeast1`. Forced: `asia-southeast1` only offers `gemini-2.5-flash`, which fails the hackathon's 3.5+ rule
- [x] **Cut Gemma?** — **Cut, 22 Aug**, then **taken up again, 31 Aug** for country discovery. The 22 Aug reasoning stands for `core/extraction/` and is not reopened: the `prefilter_sections` slot stays empty. Discovery is a different flow — it proposes and selects, never writes a clause — and Gemma is free of charge on the Gemini Developer API with no paid tier, so it cannot generate a bill.
- [x] **Cut OCR fallback. Cut, 22 Aug.** Text-layer PDFs + pasted text only; a near-empty text layer scores low `parse_quality` and the clause lands in review instead of inventing content
- [x] **The web app is a console, not a page, 29 Aug** — the frontend is laid
  out as an operations dashboard: a fixed rail, a status bar, a metric strip and
  a two-column overview. Two constraints came with it and should not be
  reopened. First, **no new frontend dependencies**: the icon set is inline SVG
  and the motion is CSS, because a font, an icon package or a motion library
  added two days before the deadline is a new way for the Cloud Build image to
  fail and buys nothing a judge can see. Second, **component CSS lives in
  Tailwind's `components` layer** — unlayered rules outrank every utility, which
  is how `hidden lg:flex` silently lost to a `display` in `globals.css`.
  Not deployed yet: the boxes above stay as they are until it is.

- [x] **The fix plan is a draft, never an action, 29 Aug** — the remediation
  endpoint and page prepare the decision (the number to hit, the market that
  sets it, the wording behind it) and stop there. No email, no state change, no
  autonomous step out of the system. That is the Collaborative Partner track's
  shape, and it is also why the feature is safe to add two days before the
  deadline: it is read-only, so it cannot break the pipeline it reads from.
  Deciding which limit is strictest is a comparison, so there is no model call
  in it — the same reason the Impact Engine has no agent
- [x] **A placeholder credential counts as no credential, 29 Aug** — an unset
  secret left at `YOUR_KEY_HERE` routed every call to an endpoint answering
  "API key not valid", and the failure was quiet: embeddings died one clause at
  a time and the app said "no regulation covers this" while holding the
  regulation. Falling back to Vertex makes a misconfiguration a cost line, not a
  wrong answer
- [x] **A citation points at a passage or at nothing, 29 Aug** — clause text is
  located inside the document and highlighted in place. Matching reports
  `exact`, `approximate` or `not_found`; an unlocatable clause is listed as
  unlocated rather than pointed at the nearest paragraph. Highlighting the
  wrong sentence would be worse than highlighting none
- [x] **Unrecognised ingredient names get an answer, not silence, 29 Aug** —
  `core/substances.py` never overrides the strict matcher; it explains its
  refusals and offers names for the user to pick. A food is called a food, and
  the sentence says out loud that nothing being checked is not a pass
- [x] **The app ships with rules, 28 Aug** — a new workspace no longer starts
  empty. `scripts/build_library.py` cuts 28 verbatim excerpts out of the two
  regulations already in `data/regulations/` and the user loads them with one
  button. They are *ingested*, not injected: same hash, same Pub/Sub message,
  same extraction, same guardrail as an upload, so nothing enters the graph
  through a path an upload could not use. Excerpts are cut, never rewritten
- [x] **A rule binds only the kind of product it was written for, 28 Aug** —
  forced by the library: with limits for every food category in the graph,
  matching on jurisdiction and substance alone failed a drink powder against a
  dairy-dessert limit. Powder and liquid stay one family (the BPOM tables say
  "dihitung terhadap produk siap konsumsi"); an unstated product type still
  binds, as the documented wildcard
- [x] **Upload metadata is read, not asked, 28 Aug** — jurisdiction, source type,
  title and effective date come from the document itself (`core/detection.py`,
  deterministic). The form asks only what could not be read, and the user can
  always override. Deliberately *not* an LLM call: upload must stay fast, and
  the source type caps what a clause is allowed to do, so it belongs in typed
  code with tests, per the standing rule that deterministic code owns every
  mutation. On a tie the lower authority wins
- [x] **Minimal onboarding is in scope, 26 Aug** — phase 7 listed "onboarding flows" as out of scope, written on the assumption that we always drive the demo. A judge opens the hosted URL cold with no product and no regulation PDF. So: bundled sample regulations, a one-click demo seed, and a three-step checklist that persists until done. No tour, no coach marks, no modal — those stay out
- [x] **Readiness as counts, not percentage?** — Counts (phase 4 will render issue counts). *User confirmation still welcome; nothing built contradicts it*
- [x] **Two GCP projects or one?** — **One** (`regulens-506014`), as already provisioned 19 Aug
- [x] **Repo public or private?** — **Public**, verified public on GitHub 19 Aug
- [x] **Composite confidence weights** (22 Aug) — `0.3·parse_quality + 0.4·self_consistency + 0.3·authority_tier`, from the concept doc's model. Self-consistency = best-match field agreement between the two Gemini samples over substance/limit/unit/product_type
- [x] **Decision events belong to reconciliation** (22 Aug) — phase 2 persists clauses as `pending_reconciliation` with no `clause_created` event; phase 3's verdict application writes exactly one of {created, superseded, conflict_opened, flagged_review} per clause. Keeps one-decision-one-event true and UC-D's zero-mutation assertion meaningful
- [x] **Upload cache key** (22 Aug) — identical bytes short-circuit when the prior document reached a terminal state (`extracted`/`reconciled`), not only `reconciled`; otherwise the cache could never hit before phase 3 exists
- [x] **Real corpus numbers override the sketch numbers** (23 Aug) — EU Annex II 14.1.4 is 150 mg/kg for E210-213; BPOM 14.1.4.x reads 400–900 mg/kg. Extraction follows the documents; the "0.05% vs 0.10%" pair stays a demo-narrative baseline seeded in phase 7, never presented as extracted fact
- [x] **Substance-family equivalence** (23 Aug) — EU limits the group "Benzoic acid — benzoates (E210-213)"; BPOM limits natrium benzoat computed "as benzoic acid". The guardrail compares across that documented shared basis (`_SUBSTANCE_FAMILIES` in `core/guardrail.py`); retrieval and requirement materialization use the same families. Not a guessed mapping — both documents state it
- [x] **Cross-jurisdiction conflict decided in code, not by the judge** (23 Aug) — different jurisdictions with different limits is deterministically a conflict; the judge is consulted ONLY for same-jurisdiction pairs whose effective dates do not settle the supersede question
- [x] **Unique image tags per deploy** (23 Aug) — Cloud Run skips revision creation on an unchanged tag, so builds pass `SHORT_SHA=<sha>-<HHMMSS>`; a plain git short-SHA silently redeploys nothing
- [x] **Gemini reached through the Developer API, not Vertex** (28 Aug) — the
  hackathon accepts "Gemini 3.5 or newer accessed through Gemini API **or**
  Vertex AI", and the Developer API's free tier covers `gemini-3.5-flash` and
  the embedding models outright. Vertex was 76% of the bill. `GEMINI_API_KEY`
  switches every call; unset, the Vertex path is unchanged, so this is one env
  var to roll back. The key must come from a project with **no billing linked**
  — linking billing promotes it to the paid tier. Cost of the switch: embedding
  vectors are model-specific, so the corpus needs `scripts/reembed.py` once
- [x] **Cloud Build runs on the default machine type** (28 Aug) — the 2,500
  free build-minutes apply to `e2-standard-2` only. The build was pinned to
  `E2_HIGHCPU_8`, which earns none of them; 23 builds cost Rp 41,070. Builds
  get slower and free
- [x] **A verification script that cannot fail is worse than no script,
  29 Aug** — `verify_e2e.sh` printed `ALL CHECKS PASSED` while a check crashed:
  every check was a pipeline ending in a Python assertion and nothing read the
  exit status. Each one now ends in `|| fail "<name>"`. The fix immediately
  exposed that the baseline check had been failing in silence on every re-run,
  because the script uploads the EU document and never removed it, so "Germany
  must read unknown" could only hold the first time it was ever run
- [x] **A document can be deleted, 29 Aug** — a product could always be deleted
  and the document you uploaded by mistake could not, so the wrong PDF stayed in
  the rulebook unless you had Firestore access. `DELETE /documents/{id}` takes
  the clauses, the requirements they produced, the conflicts they opened and the
  debug record, then re-evaluates every affected product: a delete that leaves a
  stale red verdict on screen is worse than no delete. Both verification scripts
  now clean up after themselves, which is what makes them repeatable
- [x] **Three agents run for real; the fourth is a smoke test and says so,
  29 Aug** — the repo had four ADK agents and used one. `build_query_agent` and
  `build_reconciliation_agent` were defined and never called, while the README
  table called Query "genuinely agentic". Query is now the real answer path —
  it picks its own retrieval tools, and every id its tools serve is recorded so
  the citation check has something real to validate against. Reconciliation is
  wired at the judge call and **only** there: a 55-clause annex would otherwise
  be 55 agent runs on the critical path, and the deterministic classification
  settles every other case in microseconds. Impact keeps no agent at all,
  deliberately — pass/fail against a numeric limit is arithmetic. `adk/agent.py`
  stays the phase-0 smoke test and is now labelled as one instead of counted as
  a stage. Every agent falls back to the deterministic path on failure
- [x] **An agent may not present an ungrounded answer as a grounded one,
  29 Aug** — found by the deployed E2E within minutes of wiring the query agent:
  asked about a country with no ingested regulation, it wrote "there is no
  information available" *and cited the clauses it had looked at*, so the answer
  came back `refusal: false` with citation cards under a sentence saying there
  was nothing. The agent now has one word for emptiness (`INSUFFICIENT_EVIDENCE`)
  and typed code turns that into the ordinary refusal. A single token is
  checkable; a sentence explaining itself is not
- [x] **One file, one command, 29 Aug** — `cloudbuild.yaml` hardcoded this
  deployment's own `*.run.app` hostnames, so a clone's web app called our API
  and our CORS list. `regulens.env` is now the only file anyone edits and
  `scripts/quickstart.sh` the only command anyone runs; both `*.run.app` URLs
  are computed from the project number, which Cloud Run makes knowable before
  the first deploy, so nothing is pasted by hand and the deploy stays one build
- [x] **Latency is a property of the document, not of the pipeline, 29 Aug** —
  re-measured with `scripts/measure_latency.py` rather than remembered. A single
  pasted rule is 25.5s upload→re-evaluated; the 4-page EU annex excerpt is
  174.3s, and 72% of that is extraction emitting 55 verbatim clauses. Output
  tokens are the floor. The two self-consistency samples were made concurrent
  (measured 95.9s and 110.4s inside a 110.4s window, so the sum is no longer
  paid), embeddings batched once per document instead of one call per clause,
  and the worker's blocking handlers moved off the event loop. Splitting a
  document into concurrent pieces was built and does not help this document:
  the excerpt is dense, not long, and stays one piece
- [x] **Infra stays in `asia-southeast1`** (28 Aug) — checked against the free
  tier per service. Cloud Run's free tier is region-independent and the Gemini
  API is a global endpoint. Only Cloud Storage's 5 GB is US-region-only, and
  that line was Rp 559; moving the bucket would add cross-region latency on
  every upload to save pennies. Retention rules, not relocation
## Blocked on the user

Mirrors `../todo.md`. Nothing below can be unblocked by writing code.

- [x] Eligibility confirmed (country exclusions) — confirmed by user 19 Aug
- [ ] Devpost registration — **blocks submission**
- [x] GCP project ID + region + billing — `regulens-506014`, Owner confirmed, billing linked to `01B951-232B54-4E1D9A`. Infra region `asia-southeast1`; Vertex Gemini on the `global` endpoint
- [x] Gemini 3.5+ model identifier confirmed — `gemini-3.5-flash` on `global`, verified with a live `generateContent` call; embeddings `text-multilingual-embedding-002` in `asia-southeast1`, verified with a live call
- [x] Real EU + BPOM regulation PDFs with text layers — downloaded to `data/regulations/`, see `data/regulations/SOURCES.md`
- [x] Confirmation that the two jurisdictions' limits actually differ — sodium benzoate in flavoured drinks: EU 150 mg/kg vs BPOM 400–900 mg/kg. Exact BPOM row alignment still to be confirmed against the rendered table

## Session log

Append one line per working session. Keep it short — this is for orientation, not
a diary.

| Date | Who | What changed |
|---|---|---|
| 31 Aug 2026 | bonus blog post published | Optional bonus write-up is live and recorded in `todo.md` §6. Medium: "My agent said 'I don't know' and then cited eight sources anyway" (https://medium.com/@uumaarrrrr22/my-agent-said-i-dont-know-and-then-cited-eight-sources-anyway-ef65e7f802d5); LinkedIn post: https://lnkd.in/p/gBxbjqQ4. No code change. |
| 31 Aug 2026 | the PDF reader was holding the whole book | The stage cleanup below raised the worker to 1Gi and it was **still killed**, at 1039 MiB, on the very next BPOM check. The memory was never the bug. `extract_pdf` read every page in a list comprehension while pdfplumber cached each parsed page onto an object the open document holds, so reading page 900 still held pages 1-899. Measured on the 1156-page BPOM annex already in `data/`: **5612 MB peak**, against **119 MB** once each page is flushed as its text comes out. No Cloud Run memory tier was ever going to cover the first number. Both OOMs landed while the sweep held a source's check lock, which is why a working address read as broken for hours. 1Gi stays as headroom for ADK and the Gemini client, and `cloudbuild.yaml` now says which number is the fix and which is the cushion. One test pins the call rather than the megabytes - a memory assertion would be flaky, and dropping the flush is the single edit that brings the bug back. |
| 31 Aug 2026 | stage cleanup + truth pass | The sources page had been reporting `ReadTimeout` on the BPOM listing since 06:33 while the address answered in under a second. The address was not the problem: at 13:58:05 the worker crossed 512 MiB during an ADK extraction and Cloud Run killed it **while it held that listing's check lock**, so nothing was written, the previous run's error stayed on screen as though it were current, and the 15-minute stale-lock window blocked every retry in between. Seconds before the kill that same sweep had found and ingested a BPOM regulation nobody uploaded (`doc_146418b98f63`, correctly read as carrying no product rules) — the page was contradicting the product's central claim. Worker and api moved to 1Gi, pinned in `cloudbuild.yaml` along with `--min-instances=1`, which had been set live and written down nowhere. `/sources` now computes a `checking` flag from the lock and the page says "Reading now" and marks the fields beside it as belonging to the previous run; two tests pin it, including the half that matters — a lock left by a dead process is not a read in flight. Discovery stopped calling a successful run "1 of 2 usable": a candidate turned away is the verifier working, and it now says what was kept and what was turned away. **One ticked box was false.** `scripts/setup.sh` submitted Firestore indexes with `gcloud firestore indexes create --file=`, which is not a command, and both branches of its `||` printed a reassuring sentence — a step that could never work reported success on every run since it was written. Nothing missed them: `gcloud firestore indexes composite list` returns nothing on the deployed project and every query works, because Firestore serves several equality filters from its automatic indexes and the two queries that would otherwise sort or range read a bounded page and sort in process on purpose. Script and box now say so. Also made true rather than aspirational: phases 0, 1 and 9 are complete (phase 0's four remaining boxes are one decision — Vercel and a push trigger replaced by a single Cloud Build path); phase 9 verified from Cloud Run, where Singapore committed `sfa.gov.sg/legislation` and Japan `mhlw.go.jp/hourei/`, each turning away one candidate with its reason; the clause stuck in `pending_reconciliation` since 23 Aug is gone; the review backlog is 102, not 110, with 149 more held back and stated on screen; test badge 612 → 618. Scheduler, uptime check and Secret Manager re-verified against the live project and all three hold. |
| 31 Aug 2026 | rebase onto origin | Rebased the four local commits onto `origin/master`. Two conflicts, both from the same work landing twice: `.gcloudignore` had the unanchored-`data/` fix written independently on both sides — kept the remote one, which also anchors `/plan/`, and the local commit dropped out empty — and the session log had two rows inserted at the same line, both kept. README test badge 601 → 612, the count after the discovery tests arrived from the other side. 612 passed, 1 skipped; ruff clean, tsc clean |
| 31 Aug 2026 | submission truth pass | Reconciled the two documents a judge reads with what the repo now contains. **README:** 393 tests → 601; the curing salts added to the substance-family note; Gemma and country discovery added to the stack table and the pipeline description; "sources are registered by hand" replaced with the limitation that is actually true now (a discovered source is an index to watch, not a rule, and it fails openly when a regulator publishes through a JavaScript application). One claim was worse than stale — "Answers questions with citations" reads as a product feature and is an API endpoint with no Ask box on any page, whose agent refuses a question phrased around a market even while holding the clause the product page cites. Now stated under Limitations where a reader meets it before a judge does. **Devpost description:** rewritten around the chain the product can prove — EU 2023/2108 found at CELLAR by the sweep, 88 verbatim limits, a cured sausage failed on the 30 mg/kg nitrite row in force from 9 October 2025 — with the benzoate upload story kept as the second path. Challenges now lead with `verdicts_changed: 0` and the five blockers behind it; What we learned gains the expensive one: a pipeline green stage by stage is not a pipeline that works, and four defects were only findable after a verdict finally moved |
| 31 Aug 2026 | web UI | Reskinned the console as an editorial document: warm bone canvas, white surfaces, one hairline weight (1px `#EAEAEA`), and ink as the only accent — nothing in the interface is blue any more. Page titles and quoted regulation text moved to a serif (`.t-quote`), so the face itself marks the boundary between what a document says and what ReguLens says about it; the UI stays in the platform grotesque and every number stays monospace. Status keeps four washed pastels and its words, badges stay sentence case where the label is a verdict (`.badge-tag` for one-word tags). Resting shadows dropped for hairlines, elevation only under the pointer; page gutters opened up (48/96px). No new dependencies and no fetched fonts — local stacks with system fallbacks. Verified in light and dark, desktop and mobile, against the running app; tsc clean, next build green |
| 31 Aug 2026 | the chain closed | `/stats/autonomy` had read `regulations_found: 8`, `clauses_read: 123`, `verdicts_changed: 0` since the scheduler started finding regulations. The zero was accurate and it was the product: nothing a watched source found had ever moved a verdict. Five things stood between the two numbers, each one found by simulating the recheck against the live queue before running it. **(1)** 190 EU limit rows were parked `substance_not_recognized` — read on 29 Aug, two days before the dictionary learned the curing salts — and the only auto-recheckable reason was `judge_ambiguous`. `substance_not_recognized` is now conditionally recheckable: the same strict matcher runs again over the substance the document stated, and only a name it now recognises is released. **(2)** Those rows also carried `low_confidence_or_flagged`, which is two refusals wearing one name — under the floor, or extraction flagged it — and an unrecognised substance is what raises the flag. One fact recorded twice is not two reasons; the floor is now a named constant so the gate and the recheck cannot disagree about it. **(3)** The EU annex prints no GSFA code, so every nitrite row would have looked like the same rule stated twenty-four times and gone to the judge in pairs — 96 model calls. `scope.py` now also reads the scope the row states in words (`only dry cured bacon`, `except sterilised meat products`), read and never interpreted, and the guardrail refuses `stated_scope_mismatch`. **(4)** An amendment is written as two rows, `until 9 October 2025` and `from 9 October 2025`, and only the second is an effective date — so sixteen supersedes the rows answer themselves were reaching the model. `applies_until` reads the end of the period from the text. Judge calls fell 96 → 14. **(5)** "Nitrites — Not more than 20 mg/kg expressed as KNO2" is a purity ceiling from Regulation 231/2012 naming no food, and at 20 mg/kg the strictest nitrite number in the annex; a ceiling that names no food now goes to a person. Live result: the queue fell 200 → 141 with every remaining reason named, 58 rows released, 42 active and 16 settled as supersedes by the dates the regulator printed, zero judge calls. Also found in production and fixed: a laboratory drying method (`Loss on drying — 0,25 %`) was **active** and reported as the 2 500 mg/kg limit binding a cured sausage, and could only have been removed by deleting the eighty-seven correct rules of its annex — `dismiss` now withdraws an active clause and takes its requirements and conflicts with it. And `graph.changed` carried the clause but not its document, so `cause.document_id` was null on every verdict reconciliation ever moved: the message now carries it and alerts resolve it on read, which is what `unprompted` and `verdicts_changed` are computed from. 542 tests green (26 new), ruff clean |
| 31 Aug 2026 | queue self-resolution | The review queue held 36 rows of one BPOM additive table, every one of them `judge_ambiguous`, every one of them asking a person to confirm that a regulation does not contradict itself. Cause: a clause carries a substance, a limit, a jurisdiction and a date but nothing that says *what food*, so two rows of one table read as a supersede question with no date to settle it — the single case that reaches the judge. New `core/scope.py` reads the GSFA category code the regulator prints at the head of the row (`14.1.4.2`, `04.1.2.8`); the guardrail refuses cross-branch pairs as `food_category_mismatch`, so they never reach a model and never reach a person. `POST /clauses/recheck` re-decides the backlog through the ordinary `reconcile_clause` path — `judge_ambiguous` only, never low confidence or low authority, and a clause that is still ambiguous goes back in the queue. Review page gained the button, a plain-language line for `judge_ambiguous` (it read "judge ambiguous" on screen), and a result that names what it could not settle. Proven against the Firestore emulator: fresh pair → `active` with no judge call; parked clause → `active` with `clause_rechecked` + `clause_created`; low-confidence clause untouched; integrity walker green. 503 tests green (21 new), ruff clean, tsc clean, next build green. Not deployed yet |
| 31 Aug 2026 | country discovery | Built discovery of a regulator's catalogue for any ISO country, on Gemma 4 via the Gemini Developer API (free of charge; Vertex does not serve Gemma). Measured first: the model names regulators and root domains correctly 6/6 but **every** path-carrying URL it produced was dead, 0/14 — so it is never asked for a path. It proposes the root, we fetch it, it picks the regulations index from the real link inventory, and the `link_pattern` is derived from real paths in code. Commits an ordinary LISTING source plus the market row without which `impact.py:94` would drop every clause it ingests. Yield is about one country in three; the rest render why |
| 31 Aug 2026 | test fix | `test_a_source_that_is_not_due_is_left_alone` anchored its `last_checked_at` to a hardcoded `NOW` (29 Aug) while `check_source()` reads the wall clock — the source went stale on 30 Aug and the test started failing in Cloud Build. Anchored to `datetime.now(UTC)` instead |
| 29 Aug 2026 | web UI | Rebuilt the frontend as an operations console: fixed left rail with live queue counts, sticky top bar carrying the service-reachability indicator and the two global actions, and a metric strip over an asymmetric two-column overview (verdict table left, next step / activity feed / verdict legend right). Design system retuned for density — 15px base, hairline panels, monospace figures, one accent. No new dependencies: inline SVG icon set, CSS-only motion. Every `data-testid` preserved; component CSS moved into Tailwind's `components` layer so utilities keep winning |
| 19 Aug 2026 | planning | Plan folder created; ADK + Pub/Sub + Cloud Build + observability + E2E phase incorporated |
| 19 Aug 2026 | corpus | Downloaded 5 real regulation PDFs (EU 1333/2008 original + two consolidated versions, EU 1129/2011 Annex II, BPOM 11/2019) into `data/regulations/`; text layers verified; divergence confirmed |
| 19 Aug 2026 | env audit | Verified user's todo answers: GCP project + Owner OK, GitHub repo public, gcloud/ADC OK; found billing disabled, Docker daemon down, Python 3.14 vs required 3.12; enabled `aiplatform.googleapis.com` |
| 20 Aug 2026 | repo hygiene | Rewrote the unpushed local history: `node_modules` and `.next/cache` (~150 MB, incl. an 88 MB binary) had been committed by an over-broad `git add -A`. Proper `.gitignore` written, nine commits collapsed into one clean commit, pushed |
| 19 Aug 2026 | phase 1 twin | Products collection, 20-substance normalization dictionary with EN/ID synonyms and E-numbers, unit parsing, event-writing repository, full product API, and the create/detail UI. Demo product created through the browser; `natrium benzoat` normalized to `sodium_benzoate`. Fixed a real CORS failure found by driving the form |
| 19 Aug 2026 | phase 0 web | Next.js 16 + Tailwind 4 home page rendering both markets from deployed Cloud Run; README written; uptime check on the public API; FAKE_LLM switch wired |
| 19 Aug 2026 | phase 0 build | API + worker + Job deployed to Cloud Run from one image via Cloud Build; markets seeded; Pub/Sub push round-trip proven with matching trace_id; ADK agent with a tool running on Cloud Run against gemini-3.5-flash; rollback practised |
| 19 Aug 2026 | phase 0 infra | `scripts/setup.sh` written and run: APIs, Artifact Registry, Firestore Native, GCS bucket, 4 Pub/Sub topics, 3 service accounts with narrow + bucket-scoped IAM. Push subscriptions deferred until the worker has a URL |
| 19 Aug 2026 | billing guard | $30 cap created as Rp 540,000/month (billing account is IDR-denominated) scoped to `regulens-506014`, thresholds 50/90/100%; email notification channel `7686068825666649291` for afindo.mi01@gmail.com, wired to the budget and reserved for the Phase 0 alerts |
| 19 Aug 2026 | vertex config | Billing linked by user. Found `asia-southeast1` carries only `gemini-2.5-flash` — fails the 3.5+ rule. Settled on infra in `asia-southeast1` + Gemini `gemini-3.5-flash` on the `global` endpoint + embeddings `text-multilingual-embedding-002` in `asia-southeast1`; all smoke-tested |
| 23 Aug 2026 | phases 2–5 + deploy | Phases 2–5 built AND live-verified in one push. Phase 2 `COMPLETE` (5/5 fixture accuracy on live Vertex). Live E2E green: baseline compliant→upload EU excerpt PDF→conflict opens (UC-C)→**Germany flips non_compliant unprompted** (UC-B)→alert fires→grounded query cites 2 real clauses→Japan refusal honest→cache hit→redelivery no dupes. Fixed en route: SERVER_TIMESTAMP-in-ArrayUnion, transaction.get generator API, empty clause ids published, missing clause jurisdiction/unit fields, substance-family equivalence (benzoates), judge scope narrowed to genuinely ambiguous same-jurisdiction pairs, throwaway genai clients closed (shared cached client), unique image tags per deploy. Web deployed to Cloud Run (`regulens-web`); Vercel dependency dropped; taste-skill design pass (tokens, Geist, single accent, dark/light). 62 tests green, lint clean |
| 23 Aug 2026 | drills | Two more live drills banked. **UC-D**: social-chat source produced a needs_review clause at confidence 0.47 (<0.5) with ZERO mutations to conflicts or statuses — authority tiering is demonstrably real. **Same-jurisdiction supersede**: BPOM amendment (350 mg/kg eff. 2026-12-01) superseded the active 400 mg/kg clause live (`superseded` + `superseded_by`). Two latent bugs found and fixed by these drills: cross-jurisdiction conflicts now only open against ACTIVE partners (a needs_review counterpart must not gain state), and a dominant conflict verdict no longer silently drops valid supersede findings — both findings apply |
| 23 Aug 2026 | drills + close-out | Phase 3/4/5 → COMPLETE. Drills: DLQ→failed→retry recovered live; audit-integrity walker green over live data; concurrent-delivery probe found+fixed a double-race hole; UC-F second-product propagation proven; 10/10 grounding check. Log metrics `regulens_judge_invoked` + `regulens_guardrail_rejected` created. Web: impact-chain banner + red worsening-transition highlight. Honest gap recorded: upload→flip measured 183s vs 90s target (double ADK sampling dominates). Remaining open: Cloud Build push/PR triggers (needs GitHub-app OAuth by user), docker compose (daemon down, user-deferred), formal Playwright shell, phase-7 user items |
| 25 Aug 2026 | local stack + UI | Full local stack now real and verified: `LOCAL_STORAGE_DIR` filesystem backend in `app/storage.py` (no Cloud Storage emulator exists) with the two direct-GCS readers routed through it, `FIRESTORE_DATABASE=local` for the emulator, `API_INTERNAL_URL` for server components, tracing disabled locally, jurisdiction-aware `fake_candidates`. `scripts/verify_local.sh` runs the whole drill offline: baseline → EU upload → conflict → unprompted Germany flip → alert → cache hit → redelivery-no-duplicates, all green with zero GCP calls. Web redesigned against Apple HIG for a non-technical first-time user: system type scale and colour set, translucent nav plus mobile tab bar, 44px targets, one `_ui/status.tsx` that translates every machine word once, "Start here" first-run guide, per-market verdicts on the home cards, conflicts page joined to clause text and country, plain-language failure copy. Google-font fetch dropped (system stack) so the web builds offline. 62 tests green, ruff clean, tsc clean |
| 25 Aug 2026 | UI readability pass | Audited every page at real size, light and dark. Same three defects everywhere: 13px at 60% opacity for content that had to be read, tiny grey uppercase labels doing the job of headings, clause ids in the reader's face. Fixed at the token level — solid secondary/tertiary colours chosen for contrast, 15px floor for readable text, 13px reserved for ids and coloured tertiary by definition, 20px section headings, 62ch prose measure. Requirement rows and conflict sides now show the deciding comparison as two large figures (your amount vs the limit) instead of a faint aside; ids moved behind a "Where this came from" disclosure. Home became full-width product rows with per-market tiles and a consistent alert action row. Also fixed a stepper that sat on "working…" forever: a document's status stops at `extracted` because reconciliation is per-clause, so the last stage is now derived from clause statuses and the two trailing stages collapsed into one. tsc clean, production build green |
| 25 Aug 2026 | UI round 3 | Three user-reported problems fixed. (1) Controls were different heights in the same row — an `<input>` takes its height from body line-height while a `<select>` uses the browser's, so `min-height: 44px` produced 47px and 44px side by side; every single-line control is now a fixed 48px with the native appearance removed and one themed chevron. (2) Nobody knew what to do on opening the app: home now leads with a "What to do next" card computed from real state in the order the work has to happen (broken rule → market with no rules → things to check → disagreements → all clear), and the alert list collapsed to one line each so it stops competing with it. (3) Ingredient entry was a chore: a paste-the-label mode parses a pasted list into editable rows (conservative — a number only fills in when the text is unambiguous, an unconvertible unit says so on the row), plus autocomplete from a new `GET /substances` endpoint over the normalization dictionary so users pick names that can actually match a clause. Found and fixed a real reporting bug on the way: `product_value` was rendered against the *clause's* unit, showing "0.02 mg per kg" for an ingredient given as 0.02% — impact now persists `product_unit` and the converted `comparable_value`/`comparable_limit`, and the UI shows both sides in one unit. 62 tests green, ruff clean, tsc clean, build green |
| 26 Aug 2026 | first-run self-service | Answered "what stops a user without a tutor" by building the two blockers. (1) A cold workspace demanded a regulation PDF the user does not have: `app/core/samples.py` now carries verbatim EU 1129/2011 and BPOM 11/2019 excerpts with citations, served by `GET /samples` and offered as a one-click filler on the Add-rules page; `POST /demo/seed` creates the demo product and ingests the Indonesian rule through the real Pub/Sub path (202 while extracting, 200 on repeat), idempotent by product name and content hash. The EU rule stays for the user to add, so the verdict flip is still theirs to watch. (2) The three-step guide only rendered on a completely empty workspace, so it vanished the moment the first product existed and took step two with it: it is now a checklist computed from real state (product exists / a document reached `extracted` / a market reads something other than `unknown`) that stays until all three are done, and the "What to do next" card holds back until then rather than competing with it. Seed lands on the document page, not the product, because a product page during extraction says "no rules added yet" and reads as a broken button. `job.py` de-duplicated against the new samples module. Verified from a wiped stack in the browser: empty → seed → BPOM extracted → EU sample → Germany flips `non_compliant`, one conflict open. 71 tests green (9 new), ruff clean, tsc clean, next build green. Cloud Run redeploy still pending |
| 28 Aug 2026 | self-service round two | Closed the remaining seven things a user could not do without asking someone. Editing and deleting a product (`ProductForm` now serves both, `/products/[id]/edit` finally calls the `PATCH` that had no caller, `DELETE /products/{id}` cascades derived requirements through a new `delete_with_event` so the audit event and the deletes land in one batch). Rejecting a clause (`POST /clauses/{id}/dismiss`; `dismissed` is terminal and inert, the record survives). Honest waiting: the upload button claimed "about a minute" against a measured 183s — both places now say three minutes and the document page runs a clock against it with a separate "longer than usual" state. A failing requirement says what number to get under, and names the stricter market when meeting one meets both. Nav badges for the two queues, refreshed on navigation and on queue actions; phone stays at four tabs because five labels do not fit. New `/rules` page listing every clause and why it does or does not count. `_ui/Term.tsx` glossary. Ask suggestions built from the product's own markets. Three defects found by building this and fixed: (1) a corrected ingredient amount never reached the requirement row because the idempotency check watched only limit/evaluation/severity/clause — the page kept quoting the old amount; now every displayed field is watched, with a test asserting exactly that; (2) `confirm_clause` republished on its failure branch, so accepting a clause in the review queue re-evaluated nothing, and a missing clause returned 200; (3) alerts outlived deleted products and linked to a 404. 78 tests green (7 new), ruff clean, tsc clean, build green. Verified in the browser end to end: seed, edit an amount and watch both markets re-read, dismiss from the queue, delete with the cascade, watch the stepper clock on a stalled worker |
| 28 Aug 2026 | plain-language pass | Walked every page as a non-technical reader and fixed the nine things that stop one. Biggest: "Where this came from" answered with a bare clause id on four pages — one `_ui/Provenance.tsx` now names the document and country and links to it, id demoted to a labelled reference. The review queue never said why anything was waiting at all: the UI read `review_reasons` (the extractor's list) while the guardrail writes a single `review_reason`, so every card had an empty bullet list — both are read now, with the source and confidence in words. Answers quoted raw clause ids inline; ids are renumbered against the citation cards so grounding is untouched and the sentence is readable, and "answered in 19 ms" became "answered straight away". A friendly 404 replaced the Next.js default dead end. The document page is titled with the document's name instead of "Reading your document" over an id, and a document whose rules are still waiting no longer claims "Finished" — each rule shows its standing and the panel links to the queue. History dropped its trace-id fragments and its no-op rows (`null → unknown` is a real difference in the data and the identical sentence on screen, so the filter compares rendered labels) and gained plain timestamps. Server validation stopped speaking schema: `humanizeValidation` turns "Input should be 'percent_w_w'…" into "Pick one of these for the unit on row 2: % of weight, mg per kg, ppm". Also: requirement rows sorted worst-first, a waiting rule on /rules got a button, generated `doc_x.txt` names read as "pasted text", and the local canned answer now quotes the country actually asked about. 80 tests green, ruff clean, tsc clean, build green; rechecked at phone width in light and dark |
| 28 Aug 2026 | plain-language recheck | Second pass over the first. Swept every route with a script looking for ids, snake_case, `null` and millisecond readouts in the rendered text; it found a genuine hang, not just wording: a document containing no usable rules never reached "Finished", because an empty clause list was read as "not settled yet" — the stepper sat on stage three of four indefinitely for anyone who uploaded something that turned out not to be a regulation. It now completes and says what happened, and stops claiming "Rules found" over an empty list. Added the last machine words to the vocabulary map (`dismissed`, `pending_reconciliation`, the three evaluation reasons). Sweep is clean: the only ids left anywhere are inside a collapsed "Where this came from", labelled as the reference to quote to us. Verified the alert acknowledge, the review-queue actions, the sample flow and the first-run seed once more from a wiped stack |
| 28 Aug 2026 | the form stops interrogating people | Step two used to demand four answers before it would take a document: authority tier, jurisdiction, publisher, effective date. The document answers all four itself, so `app/core/detection.py` now reads them — deterministic keyword weights and date patterns, no model call, so upload stays fast and a wrong guess cannot quietly widen what a clause may do. Every answer carries the phrase it was read from, and the UI quotes it back (“because it says ‹Official Journal of the European Union›”). Uncertainty is a real answer: scores are separated rather than summed, so a document naming both regulators comes back unsure instead of confidently wrong, and on a tie the lower authority wins — a forwarded screenshot quoting BPOM reads as a chat message, not as the regulation. A date is only claimed where the document ties it to taking effect, in either language. New `POST /documents/detect` reads a file or pasted text and stores nothing, so changing your mind leaves no document and no file behind; `POST /documents` metadata went optional, refusing only an unreadable jurisdiction or source type and saying what to do about it. The record keeps the detection and a `declared_fields` list, so “who said this was EU” has an answer, and the document page says so when nobody typed it in. The form was rebuilt around the document: drop it in, we read it, we show what we read; the five-way authority picker and the three fields appear only when we could not read them or the user says something is wrong. 99 tests green (19 new), ruff clean, tsc clean, next build green. Rechecked on the local stack: both bundled excerpts and the 1333/2008 PDF read correctly with no input at all, and a metadata-free upload ran through the pipeline to `extracted`. Cloud Run redeploy still pending |
| 28 Aug 2026 | cost control | Bill read: Vertex AI Rp 177,395 (76%), Cloud Build Rp 41,070 (18%), everything else inside the free tier. Two root causes, both fixed. (1) The build was pinned to `E2_HIGHCPU_8`; the 2,500 free build-minutes apply only to `e2-standard-2`, so all 107 measured build-minutes billed. Override removed. (2) Vertex bills every token while the Gemini Developer API's free tier covers `gemini-3.5-flash` and embeddings, and the rules accept either — `GEMINI_API_KEY` now routes both the generation client (`core/extraction/llm.py`) and the embedding call (`core/reconciliation.py`), falling back to Vertex when unset. `scripts/reembed.py` re-embeds the corpus, which the switch requires because vectors do not transfer between models. Also capped Cloud Run at `--max-instances=3` (was 20) and wrote `scripts/gcp/` retention rules: 7-day uploads, 3-day build sources, keep-3-versions in Artifact Registry (0.5 GB free). Region audited against the free tier and deliberately unchanged. **Not applied: billing is currently unlinked from `regulens-506014`, so the three retention commands return `BILLING_DISABLED` — run `scripts/gcp/apply-cost-controls.sh` after relinking.** 219 tests green. Pre-existing blocker for the next deploy: 12 `E501` lint errors in the untracked `scripts/build_library.py` will fail the build's lint step |
| 28 Aug 2026 | bundled rule library | Answered "why do I have to add a regulation?" — because we shipped an empty rulebook and sent people to find a PDF, which is the job they came here to avoid. Two real regulations were already in the repo, so `scripts/build_library.py` now cuts 28 verbatim excerpts out of them (12 EU Annex II food categories, 16 BPOM additive sections), each with its citation, and `POST /library/load` reads the starter set of 8 — or any subset — through the ordinary upload path. The EU side is built from the EUR-Lex **HTML** of the consolidated 1333/2008 rather than the PDF, because a text dump of the PDF unaligns the table columns and an unaligned row pairs a category with the wrong number; the BPOM side uses pdfplumber, which keeps each annex row intact. That resolves the row-alignment caveat standing in SOURCES.md since 19 August. Buttons on the home checklist, the product page's empty state, and the add-rules page, which now leads with the rulebook. Two things the library exposed and forced fixes for: (1) numeric limits bound to products they do not cover — matching used jurisdiction and substance only, so a drink powder was failed against the benzoate limit for dairy desserts; limits now bind only when the clause's product type is comparable, with powder and liquid as one documented family; (2) the substance dictionary knew 21 names against a library that quotes 45, and an unmatched substance reads on screen exactly like passing. `FAKE_LLM` now reads the excerpt's own table instead of answering every document with the same canned pair. Documents record their origin so a rulebook entry reads "from the built-in rules", not "pasted text". 223 tests green (45 new), ruff clean, tsc clean. Verified from a wiped stack: seed + load → 9 documents, 200 clauses, 8 conflicts, Germany non_compliant at 150 mg/kg and Indonesia compliant at 400 for the same product; `verify_local.sh` still passes. Cloud Run redeploy still pending |
| 29 Aug 2026 | citations and ingredient understanding | Three things the rulebook made necessary. (1) "Where this came from" now shows the passage: `GET /documents/{id}/text` returns the document's words with every clause located inside them, the document page highlights them in place, and a rule links to `?cite=<clause_id>` which opens the reader scrolled to its sentence. Matching reports exact / approximate / not_found — an unlocatable clause is listed as unlocated rather than pointed at the nearest paragraph. Extracted PDF text is persisted now (capped at 200k characters) so there is something to show, and the offline reader quotes rows verbatim again after a regression that prefixed the substance onto them and made its own clauses unfindable in their own document; 328 of 328 citations across the loaded rulebook resolve exact. (2) A market shows one card per ingredient instead of six: with a rulebook loaded, several limits cover the same substance, so the strictest (or failing) one leads and names its rule, and the rest collapse into "N more limits for this ingredient here", each linking to its own passage. Fixed a real defect on the way — the remediation line compared a market against itself, so a German rule could read "Germany is stricter still at 150"; the target is now the strictest limit in the same market, and only other markets can be stricter still. (3) `GET /substances/resolve` answers what a user meant by an ingredient name, because a name we do not know matches nothing and reads exactly like a pass: a misspelling offers the name back, an E/INS number resolves however it is written, a food is told it is a food ("meat", "daging ayam" — and the sentence says this is not a pass), a function word ("preservative") is asked which substance it is, and a genuinely unknown name is told plainly that nothing will be checked against it. Foods the dictionary already knew — ginger, turmeric, sucrose — are labelled foods, because promising a check for them invents a rule that does not exist. Every ingredient row runs it on a pause in typing; suggestions are buttons, never applied on their own. 252 tests green (29 new), ruff clean, tsc clean, next build green, `verify_local.sh` passes from a wiped stack. Cloud Run redeploy still pending |
| 29 Aug 2026 | deployed end-to-end run | Deployed everything to Cloud Run and exercised it live with real Gemini, no FAKE_LLM. Cloud Build green (lint, 257 tests, api + worker + job + web); the first build failed on lint over a stray duplicate of the library builder inside `api/`, now removed. Live results: detection on a real PDF reads EU/official regulation off the masthead; an upload with no metadata at all extracts 34 clauses; three library entries return 59/45/15 clauses with units taken from the table headers; citations locate 149 of 151 clauses on every document stored since the full text started being kept, and the one document at zero predates that and says so; grounded Ask cites the failing limit with refusal:false at ~4s; identical bytes hit the upload cache; a republished `document.uploaded` left the clause count unchanged; every web route serves 200 and the citation reader opens on the passage the link names. Three real defects found and fixed. (1) The `gemini-api-key` secret held the literal placeholder `YOUR_KEY_HERE` — any non-empty value flipped every call to the Gemini Developer API, which answered "API key not valid", and the damage was silent: embeddings failed one clause at a time, similarity search degraded to nothing, and Ask answered "no regulation covers this" while holding the regulation. A placeholder now counts as no key, so the stack falls back to Vertex. (2) 151 clauses left without embeddings by that window were backfilled with `scripts/reembed.py` against the deployed Firestore (151 done, 51 already current, 0 failed), after which Ask started answering. (3) `mg/kg or mg/l` was rejected as an unusable unit while `mg/l or mg/kg` was accepted — the same header the other way round, sending real limits to review; rows carrying no number at all no longer claim a units problem either. CORS also fixed: Cloud Run answers on two hostnames and only one was allowed. Note for the next session: `api/scripts/reembed.py` was deleted by accident during that lint cleanup and rewritten from what `embed_text` documents — check it against your own copy if you have one. 257 tests green, ruff clean, tsc clean |
| 29 Aug 2026 | what the E2E was hiding | Turned the verification suite on itself and it failed honestly for the first time. **(1)** `verify_e2e.sh` printed `ALL CHECKS PASSED` while a check crashed — a `/query` call came back empty, its Python parser raised, and nothing read the pipeline's exit status. Every check now ends in `\|\| fail "<name>"`, and the query calls carry `--max-time 120` so a hung request fails loudly instead of piping nothing into a JSON parser. **(2)** That fix immediately exposed a check that had been failing in silence on every re-run: the drill asserts Germany starts at `unknown`, but the drill itself uploads the EU document and never removed it, so the assertion could only hold on the first run ever. The script cleans up after itself now. It also states the precondition out loud — a workspace that has loaded the bundled rulebook already holds EU rules, so the unprompted flip has already happened and cannot be observed there; it exits non-zero saying so and points at `verify_local.sh`, which gets a wiped stack every time. **(3)** The cleanup needed something that did not exist: you could delete a product but never a document. `DELETE /documents/{id}` now removes the document, its clauses, the requirements they produced, the conflicts they opened on either side, and the debug record, then re-evaluates every affected product. Six unit tests pin the cascade, including the one that broke in practice — a conflict between two clauses of the same document is found once per side, and Firestore rejects a batch deleting one reference twice. **(4)** Used it to clean the demo workspace, after checking they really were duplicates: six near-identical copies of the same EU excerpt left by latency runs, sharing 39/39 numeric limits with the survivor, differing only in `(substance, None, None)` rows that one extraction emitted and another did not. Conflicts 10 → 3. `measure_latency.py` and `verify_e2e.sh` both delete what they upload now (`KEEP=1` to inspect). **(5)** Query latency, measured: the agent path cost 42s. Repeat searches inside a run are answered from the first result, and the retrieval the caller already did is handed to the agent up front so searching is no longer compulsory — 19–28s now, refusal and grounding both re-verified live. Recorded in the README as measured, not rounded down. 279 tests green, ruff clean, `verify_local.sh` green from a wiped stack. |
| 29 Aug 2026 | the agents, the hardcoding, and the clone test | Four questions, four answers, all verified against the deployed stack. **(1) Why only one agent?** Because two of the four were dead code: `build_query_agent` and `build_reconciliation_agent` were defined and never called, while the README called Query "genuinely agentic". Query is now the real `/query` path — logs from the live run show it choosing among its four tools (`find_clauses`, `get_conflicts`, `get_product_compliance`, `get_events`), calling them repeatedly, serving 79–113 clause ids and citing 5–8. Every id a tool serves is recorded on a ContextVar so the citation check validates against clauses this process actually read, not against a prompt's promise. Reconciliation is wired at the judge call and only there — verified live by uploading a same-jurisdiction amendment with no effective date, which produced `judge agent complete tool_calls=check_comparability_tool verdict=distinct_scope` feeding the deterministic `judge_invoked`. Impact still has no agent and no model call, on purpose. `adk/agent.py` is the phase-0 smoke test and is now labelled as one. **(2) A regression the E2E caught immediately:** the query agent, asked about a country with no ingested regulation, said "there is no information available" *and cited the clauses it had looked at*, returning `refusal: false` with citation cards under a sentence saying there was nothing. Fixed with one checkable token (`INSUFFICIENT_EVIDENCE`) that typed code turns into the ordinary refusal; re-verified live, Japan refuses with 0 citations. **(3) Hardcoding:** `cloudbuild.yaml` carried this deployment's own `*.run.app` hostnames, so a clone would have built its web app against our API and our CORS list — they are `_API_URL` and `_WEB_ORIGINS` substitutions now, image paths use `${_REGION}`, `settings.project_id` defaults to a name that does not exist rather than ours, and the uploads bucket derives from the project id. **(4) Clone experience:** one file (`regulens.env`, gitignored, example committed) and one command (`scripts/quickstart.sh`), which provisions, stores the key, deploys, wires push subscriptions, fixes CORS to whatever hostnames Cloud Run actually handed out, and seeds. Both URLs are computed from the project number, so nothing is pasted by hand and it stays a single build. Ran it end to end against the live project: exit 0, both web hostnames 200, api healthy. Diagram redrawn with the three agents where they run, plus an explicit "impact: no model call" edge. Full `verify_e2e.sh` green on the deployed stack; `verify_local.sh` green from a wiped stack; 273 tests, ruff clean, tsc clean. |
| 29 Aug 2026 | latency, measured instead of remembered | Went after the honest-limitations list and found only one item that was a defect rather than a decision: the latency line. Four real serialisations removed. (1) The worker's `async def` push handlers called blocking work — `reconcile_clause`, `run_impact`, extraction — directly on the event loop, so one instance handled one clause at a time no matter how many deliveries were in flight and a document's clauses reconciled in single file; all three now go through `run_in_threadpool`, and `run_extraction_via_agent`'s blocking sections through `asyncio.to_thread`. (2) The two self-consistency samples ran one after the other; both paths now run them together, measured live at 95.9s and 110.4s inside a 110.4s window, so the sum is no longer paid. (3) Embeddings were one HTTP call per clause inside reconciliation, each building a fresh `genai.Client`; there is now a cached client and a batched `embed_texts`, called once per document at extraction and written in one Firestore batch, with the per-clause path kept as the fallback. (4) The ADK tool re-downloaded and re-parsed the PDF once per agent run on top of the pipeline's own parse — three passes over 700 KB — and now reads the text the pipeline already stored. Also built and kept: `split_for_extraction`, which fans a long document out into concurrent pieces. It does **not** help the demo document and the note says so — the 4-page annex excerpt is under 12,000 characters, so it stays one piece; it is dense, not long. New `scripts/measure_latency.py` makes the README's number reproducible instead of remembered: one pasted rule 25.5s upload→re-evaluated, the 4-page annex 174.3s (extract 125.7s, reconcile 47.9s, impact 0.7s), two runs apart by 5%. The 90s target is met for an announcement and missed for an annex, and the README now says which and why rather than quoting one number. README's limitations list also split in two: things the system does not do, versus deliberate choices (substance families, issue counts) that were being read as shortfalls. 268 tests green (4 new files' worth of concurrency and chunking contracts), ruff clean, `verify_local.sh` green from a wiped stack, deployed and measured live twice. |
| 29 Aug 2026 | secrets audit + README + architecture diagram | Swept the repo for secrets: only real gap was `api/.env` (live `GEMINI_API_KEY`) sitting outside `.gitignore` — never staged, history clean, no hardcoded keys anywhere in source. `.gitignore` now excludes `.env` / `.env.*` (keeps `*.example`), `*.key`, `*-credentials.json`, ADC json. README brought back in line with the code: core loop names the deterministic detection pass and the bundled library; Architecture section embeds a generated `docs/architecture.png` (new `docs/architecture.py`, `mingrammer/diagrams` with GCP nodes — Cloud Run x4, Pub/Sub + DLQ, Firestore, GCS, Secret Manager, Vertex/Gemini, Artifact Registry, Cloud Build, Cloud Logging); tech stack states the dual Vertex-or-Gemini-API path; repo layout adds detection/citations/library/substances/integrity; test count 62 → 257; the false "Secrets: none exist" replaced with the truth (`gemini-api-key` in Secret Manager, mounted via `--set-secrets`); cloud-deploy steps fixed (`$YOUR_PROJECT_ID` typo, plus the missing secret-create + `secretmanager.secretAccessor` grant that `scripts/setup.sh` never did). |
| 29 Aug 2026 | watched sources — the app stopped waiting to be told | Until now nothing entered the graph unless a person uploaded it, which made the honest word for the product "checker", not "monitor". `/sources` is a watch list: a regulator's own address, re-read by Cloud Scheduler at 06:00 Asia/Jakarta, ingested through the same `create_document` an upload calls — same hash, same Pub/Sub message, same guardrail, same review queue, no back door. **The loop closed in production**: the scheduler fired, the worker swept under OIDC, Commission Regulation (EU) 2023/2108 was fetched, extracted by Gemini, and 68 clauses now exist that nobody uploaded (67 in review, 1 active — the guardrail doing its job on an unfamiliar annex). Three things were learned the expensive way. **(1) A change is a change of wording, not of bytes.** EUR-Lex stamps a fresh session id into every response, so a byte hash would report a change and bill a model run every single night; the signal is a hash of the extracted text, with conditional GET short-circuiting ahead of it. Three consecutive live checks: `changed`, `unchanged`, `unchanged`. **(2) What works from a laptop is not evidence it works from Cloud Run.** The EUR-Lex URL was verified locally three times; deployed, it answered `202` with a 2 KB challenge page and the first scheduled sweep recorded an error. Switched to CELLAR, the Publications Office's machine-readable endpoint — same 48,417 characters, and it sends an `ETag`, so the daily check now transfers no body at all. Two fixes came out of it: an empty 2xx body is its own named failure instead of being reported as "a login page", and `Accept` is sent explicitly, because CELLAR content-negotiates and a request without it returns 124,304 characters of RDF *about* the regulation, which reached extraction once and failed there. **(3) A pre-existing bug this feature made reproducible:** `Cannot send a request, as the client has been closed` (also in the logs for 23 and 28 Aug). `google.genai`'s `BaseApiClient` closes its httpx client on garbage collection unless the caller supplied one, so the direct extraction fallback died on a transport a finished ADK runner had shut and a document with a working path left was recorded `failed`. Both clients are handed a transport this process owns now; the failed document retried straight through to `extracted`. Also: a feed's first read ingests nothing and records what it already carried (adopting a feed means "tell me what happens next"), bursts are capped at three entries per run, a failed entry stays new so it is retried, an oversized document is refused rather than truncated, and the check lock is a Firestore transaction because Cloud Scheduler retries. Honest gap recorded: watching one broad annex put 68 clauses into the review queue, most of them cured-meat nitrite limits no product resembles — correct behaviour and an unreadable queue; a per-source relevance filter is not built. 339 tests green (55 new), ruff clean, tsc clean, `next build` clean. |
| 29 Aug 2026 | discovery, not just change-watching | Answer to "what if the new rule is published at a different URL?" — which the day's first build did not cover. A `document` source can only ever report that a rule already held was edited; a regulation published tomorrow arrives at an address nobody has seen. Added a third source kind, `listing`: a regulator's index page plus a required pattern saying which of its links are regulations, with any unseen link treated as a new document. The pattern is required, not defaulted — an index also links to news, social media and a language switcher, and following all of them would ingest the website — and a pattern that matches nothing is reported as an error, because a site redesign and a quiet month must not look identical. Feeds and listings now share one item-check routine; only how the list is obtained differs. **Proven in production against the live BPOM portal**: `jdih.pom.go.id` baselined at 12 links from Cloud Run, then with two forgotten (standing in for an overnight publication) the next check returned `changed, new_entries: 2` and ingested both — Keputusan 366/2026 and 104/2022, titles read out of the URL path because the index links are icons with no text — and both reached `extracted`. Second fix the same session: `FetchError` now carries `permanent`. A 308-page annex will be 308 pages tomorrow, so the refusal is final and the item is marked seen; a timeout or an empty page is not, because EUR-Lex serves empty challenge pages and dropping a regulation over one bad night is the worse mistake. Without it the same oversized PDF is downloaded and refused every night forever, holding a slot in the per-run cap. EU discovery is available and deliberately unseeded: EUR-Lex serves a saved search as RSS, which plugs into `feed` with no code, but the feed id belongs to a EUR-Lex account so there is nothing generic to ship; the CELLAR SPARQL endpoint was verified answering date-filtered queries and left unbuilt rather than become a fourth kind serving one publisher. Honest gap: a listing only sees what its index shows — JDIH's front page carries twelve items, so a regulation published and pushed off between checks is missed. 354 tests green (15 new), ruff clean, tsc clean. |
| 29 Aug 2026 | EU discovery, and the diagram redrawn | Follow-up to the same question — a new rule at a new URL — for the market the listing did not cover. EUR-Lex cannot be watched at all from Cloud Run (it answers a datacentre address with a challenge page), so the EU is watched by asking its catalogue instead of scraping its site: a fourth source kind, `sparql`, against the Publications Office endpoint. The query was built empirically against the live endpoint rather than guessed — found the EuroVoc concept for *food additive* (`eurovoc.europa.eu/6052`), measured the volume it carries (133 works a year), discovered that the type predicate is `work_has_resource-type` with a hyphen and that the clean filter is the CELEX shape itself (`^3\\d{4}R\\d{4}$`, which keeps acts and drops merger notices, proposals and corrigendum notices), and verified the result: ~12 real food-additive regulations a year, each expanding to `publications.europa.eu/resource/celex/<id>` — the same address shape already proven to serve regulation text from Cloud Run. Three design calls recorded: the window is a fixed 120-day lookback rather than "since the last check", so a missed run or a restored backup cannot open a silent gap; a query that selects no fetchable column is refused when it is typed, not at 06:00 in a log; and a CELEX id is an identifier, not a name, so catalogue items carry `prefer_detected_title` and let the act state its own title. Feeds, listings and catalogues now share one item-check routine — only how the list is obtained differs. Both markets can now discover a regulation nobody knew about; before today, watching a document you already held could only report that it was edited. README rewritten (four source kinds, a two-ways-in pipeline block, the seeded watch list as a table, and the new scope limits stated), and `docs/architecture.py` redrawn: a "Watched sources (the open web)" cluster with Cloud Scheduler and the four addresses, the OIDC sweep edge into the worker, the conditional GET out to the regulators, and the `create_document` edge landing on the existing Pub/Sub topics rather than opening a second way into the graph. 369 tests green (15 new), ruff clean, tsc clean. |
| 30 Aug 2026 | the queue, the payoff, and a rubric audit | Two things the watched sources exposed, plus a pass over the judging criteria. **(1) The review queue had reached 200 entries** — every one correct, 164 of them about ingredients nothing in the workspace contains, which is the guardrail working and a queue nobody reads. `core/relevance.py` narrows it to what could bear on products that exist, computed on every read so it can never go stale: 200 → 36 in production, with `164 about ingredients none of your products contain` stated on the page and a button to show them anyway. Nothing is deleted or downgraded, and the filter is deliberately generous — an unstated substance or product type still binds, and an empty workspace filters nothing. **(2) An alert now says why.** It read "changed by a rule you added, without being asked" for every alert, which was wrong for an upload and wrong in the other direction for a regulation found overnight. `core/alerts.py` resolves the cause from stored records into facts — which regulation, which market, which limit, and whether anybody uploaded it — and admits a deleted cause instead of inventing one. `GET /stats/autonomy` counts the same claim: regulations found unprompted, rules read, verdicts moved, every figure a query rather than a counter that could drift. **(3) Audited the repo against the judging rubric.** Confirmed by grep rather than assertion that `api/app/core/` imports neither FastAPI nor ADK, and that ADK is confined to four files. Closed four gaps: `make test-all` (one command — lint, tests, tsc, a production web build and the full local emulator drill, printing a pass/fail table, green from a clean checkout), `firestore.indexes.json` with the three compound queries and idempotent submission from `setup.sh`, `min-instances=1` on the two services a person opens so a first click never pays a container start, and the stale Secret Manager checkbox reconciled with reality. Honest gap left standing and recorded: no watched source currently affects the demo product, so no alert is `unprompted` in this workspace yet — the mechanism is tested and deployed, but the completed chain is not demonstrable until a regulation that touches a drink powder is published or watched. 392 tests green (23 new), ruff clean, tsc clean, `make test-all` all green. |
| 30 Aug 2026 | README rewrite | README restructured as a conventional open-source landing page: logo (`docs/logo.svg`, new), badge row, in-page nav, Features / Quick start / Architecture / How it works / Deploy / Configuration / Project structure / Limitations / Resources, GitHub admonitions for the three things a reader gets wrong (local mode is FAKE_LLM, `setup.sh` is idempotent, latency depends on clause count). Four claims corrected against the code rather than carried over: discovery is three of four source kinds, not two (`feed`, `listing`, `sparql`); the test count is 393, not 392; the deploy is three Cloud Run services plus one Job, not "four services from one image"; and the quick-start first step is **Try it with sample data** (`POST /demo/seed`, which seeds markets itself), not "Add rules". Live stack re-checked while writing: `/health` 200 at version `de5e7b7`. No code changed. |
| 30 Aug 2026 | time-aware compliance | `effective_date` was extracted, stored and used only to order supersessions; `core/impact.py` never read it. So a limit entering into force in 2027 marked a product `non_compliant` **today**, and — the worse half — a limit binding in 60 days was invisible for 60 days, which is the one window a company could still reformulate in. The verdict is now asked as of a date: `rollup_status(product_id, as_of)` counts only rules in force, and `upcoming_changes()` returns, per market, the next date the answer changes, what it changes to, and **which clause and document set it**. Three deliberate calls, each failing toward showing a real limit rather than hiding it: an absent date binds now (every clause stored before today looks like that), an unreadable date binds now too (failing closed would drop a real limit because a string was malformed), and a market whose rules are all future-dated reads `compliant`, not `unknown` — nothing in force is being broken, and "we have read no regulation" would be false. A new `product_status_scheduled` event carries the deadline into the audit trail and the alerts banner; `alerts.worsened` decides which of them is news, so a cleared deadline lands in the trail without raising anything. **Review on the deployed stack caught a defect the tests had not**: the scheduled event was written with `cause=None`, so the banner rendered "the rule behind this has since been removed" about a rule that exists — an alert that cannot name its cause is indistinguishable from one whose cause was deleted. `upcoming_changes` now names the worst-evaluating rule starting that day. Two additions outside the original plan, both to verify rather than assert: `FAKE_LLM` reads an explicit `shall apply from YYYY-MM-DD` (without it the local stack cannot produce a future-dated clause at all, so the feature would only be testable against a paid model), and `verify_local.sh` gained a step that uploads one and asserts today's verdict holds while the deadline appears and names its rule. **Verified live** on `a05be18-175751`: Gemini read `2027-01-12` out of prose, today's verdict held at `attention_required`, `upcoming` reported `non_compliant` from that date with its clause and document, and the alert came back `cause_available: True`. Test product and test document deleted afterwards. 415 tests green (22 new), ruff clean, tsc clean, `make test-all` all green. |
| 30 Aug 2026 | the rest of the improvement backlog | Seven items, commissioned in one go and worked in dependency order. **Strictest-wins** (`core/strictest.py`): the graph could say "the EU allows 150 and Indonesia allows 400, and those disagree", which a company with one recipe cannot act on. Per substance it now gives the lowest limit still in force across the markets a product actually targets, which jurisdiction sets it, the spread beside the winner, and — named out loud — the markets that have no rule for it, because "strictest of the markets that regulate it" and "strictest of everywhere you sell" are different claims. **What-if** (`POST /simulate`): a verdict for a product nobody saved, sharing `clause_binds` and `evaluate` with the impact engine so a preview cannot drift from the page it previews; pinned by a test that makes `write_with_event` raise. **Long documents.** This and "latency by fan-out" turned out to be one problem, and phase 4's own measurement said why: chunking and in-request concurrency already existed and did not move the number, because the annex was dense, not long. The wall is that the worker answers a push inside **300 seconds**, so raising the size refusal alone would have turned a named refusal into an unexplained timeout. `core/extraction/fanout.py` publishes one `document.chunk` per piece, each with its own request budget; the last piece to arrive reduces them through the same gate a short document uses. Additive by construction — the decision reads the `char_count` stored at upload, so an ordinary document takes exactly the path it took before. Three at-least-once hazards covered: a piece delivered twice overwrites itself, two pieces finishing at once race for a transactional claim and exactly one wins, and a missing piece leaves the document unfinished with a count rather than producing clauses from the parts that landed. `max_fetch_chars` 200,000 → 400,000; still a refusal above it, never a truncation. **Alerts leave the building** (`core/notifications.py`): a JSON POST to a configured webhook — which is what Slack and Discord incoming hooks are — off unless `ALERT_WEBHOOK_URL` is set, marked on the causing event so redelivery cannot double-send, capped per run, and never able to fail the pipeline that produced the verdict. Email skipped on purpose: no SMTP provider and no verified sender exist, and an untested mail path is a claim rather than a feature. **Measured accuracy** (`core/evaluation.py`, `make eval`): "5/5 on fixtures" counted a right answer beside four inventions as perfect, because it stopped at the first match and never counted false positives. Now precision, recall and F1, with `n` on every row, hand-labelled sets for the guardrail, unit conversion and the evaluator, and the scorer itself under test — a quietly wrong scoring function makes every number downstream wrong and confident. It caught a bad label on its first run (`g_per_kg`, a unit this system does not accept; refusing it is correct). Live extraction precision is **not** re-measured this session — that needs `REGULENS_EVAL=1` and real model calls. **Evidence pack** (`GET /products/{id}/evidence`): every verdict with the rule as the regulator wrote it, its confidence breakdown, the source and its stored `content_sha256`, the comparison in both forms (0.03% *and* 300 mg/kg), and the stored audit trail. A verdict whose clause was deleted is marked, not dropped. The pack states what it is not — not signed, only what ReguLens read, and `needs_review` is not an assertion of compliance. 466 tests green (52 new), ruff clean, tsc clean, `make test-all` all six rows green including three new drill steps. |
| 29 Aug 2026 | submission prep | Reconciled every checkbox with what is actually deployed: Secret Manager ticked with evidence, phase 6 status aligned with its own drill record, the five-alert and push-trigger lines turned into decisions instead of open debt, README leads with video + live link |
| 29 Aug 2026 | S1 remediation | Turned the alert into a decision. New read-only `GET /products/{id}/remediation` over `api/app/core/remediation.py`: groups requirements by substance, takes the strictest `comparable_limit` across the markets the product actually sells into (requirements for markets it has since dropped are excluded, or an old market's limit would tighten the number), names the market that sets it, and carries each limit's verbatim clause, effective date and passage link. No model call anywhere in it — same reason the Impact Engine has no agent. No Firestore write, no publish, no `graph_event`; the test's stand-in Firestore has no write methods, so adding one fails the suite. Pydantic refuses a target with no number and no reason for having none; a market we hold no rule for is named with `coverage: partial`, and every ingredient nothing was compared against is listed in `not_checked` with the reason from `core/substances.py` (a food is called a food). New page `/products/[id]/remediation`: the answer in one sentence, a per-market table with the strictest flagged, the quotes linked to their passages, "what we did not check" in the open, and Print/Save-as-PDF through `window.print()` + `@media print` — no PDF library. "Prepare a fix plan" appears on the failing market card and the alert row only when something actually fails. Framed for the Collaborative Partner track in as many words: "This is a draft for you to check, not an action we took." 297 tests green (14 new), ruff clean, tsc clean, next build green, `verify_local.sh` still green from a wiped stack; checked in the browser light/dark at phone and desktop width against the demo product (target `150 mg per kg`, both clauses quoted). One defect only the deployed workspace could show: targets keyed on the raw `substance_normalized` split the EU's "Benzoic acid — benzoates" from BPOM's "natrium benzoat, computed as benzoic acid" into two targets, each quoting one market and each claiming the other market had no rule — grouping is family-aware now, with tests for the merge and for it not merging wider. |
| 31 Aug 2026 | submission drafts | Devpost registration and profile confirmed done, boxes ticked in `todo.md`. Drafted `plan/submission/DEVPOST-DESCRIPTION.md` (full form text: inspiration, what it does, how built, technologies, named data sources with CELEX ids, challenges, learnings, built-with) and `plan/submission/VIDEO-SCRIPT.md` (4:05 beat sheet with timings, including the 35s Google Cloud proof segment the rules require). Both carry the **real** figures — product 300 mg/kg, BPOM 400, EU 150 — superseding the 0.10%/0.05%/0.08% still in `99-demo-script.md`, which predates the library cut. Video, screenshots and the Devpost form itself remain outstanding |
