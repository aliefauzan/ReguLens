# Local development only. A deployed web service gets this baked in at build
# time by cloudbuild.yaml, from the URL scripts/quickstart.sh computes — there
# is no *.run.app hostname to copy by hand.
NEXT_PUBLIC_API_URL=http://localhost:8080
